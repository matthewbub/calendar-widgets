{
  "name": "root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install:docs": "cd ./workspaces/docs && pnpm install",
    "install:calendar-widgets-tests": "cd ./workspaces/calendar-widgets-tests && pnpm install",
    "install:calendar-widgets": "cd ./workspaces/calendar-widgets && pnpm install",
    "new": "pnpm run install:calendar-widgets-tests && pnpm run install:calendar-widgets && pnpm run install:docs"
  }
}